# yaml-language-server: $schema=https://json.schemastore.org/docker-compose.json
name: splasher-lol

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8022:8022"
    env_file:
      - .env
