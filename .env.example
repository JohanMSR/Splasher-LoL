# Riot Games API — create a key at https://developer.riotgames.com/
# Copy this file to `.env` or `.env.local` and paste your key.
RIOT_API_KEY=

# Optional: pin Data Dragon version (e.g. 15.1.1). Leave empty to use latest from versions.json.
NEXT_PUBLIC_DDRAGON_VERSION=
